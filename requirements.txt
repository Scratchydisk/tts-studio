# Core dependencies
torch>=2.0
torchaudio>=2.0
gradio>=5.25
numpy
soundfile
scipy
huggingface-hub

# F5-TTS
f5-tts

# Dia-1.6B
nari-tts @ git+https://github.com/nari-labs/dia.git
descript-audio-codec>=1.0.0

# Kokoro-82M
kokoro>=0.9.4
misaki[en]

# Spark-TTS-0.5B (no pip package — repo is cloned at runtime)
einops
omegaconf
transformers>=4.46
soxr
safetensors

# Orpheus-3B
orpheus-speech
vllm>=0.7.3
snac

# Voxtral-4B (uses HTTP API against a vLLM server; httpx comes with gradio)

# Caption pipeline
pyyaml>=6.0
# ffmpeg required on system PATH
